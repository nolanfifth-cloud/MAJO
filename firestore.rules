rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Firebase Data tab and the app need immediate access without being blocked by auth context.
    allow read, write: if true;
  }
}

